# MajiOS Core

Owns the MAJI identity layer.

Loads when:
- `MAJI` is activated
- session continuity matters
- save routing needs a durable destination

Stores:
- identity
- relationship memory
- session memory
- diary and save behavior

Primary location:
- `maji/`
