# MajiOS Engine

Owns persistent memory runtime and retrieval logic.

Loads when:
- conversation history or durable recall is needed
- write-memory or ranking logic matters
- system startup needs project memory context

Primary location:
- `memory/`
